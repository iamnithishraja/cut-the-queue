{
  "name": "consumers",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc -b"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/node": "^22.7.5",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.3",
    "bandwidth-sdk": "^3.1.2",
    "kafkajs": "^2.2.4",
    "zod": "^3.23.8"
  }
}
