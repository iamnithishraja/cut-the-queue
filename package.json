{
  "name": "cut-the-queue",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "packageManager": "pnpm@9.4.0",
  "scripts": {
    "prisma:migrate": "pnpm --filter @cut-the-queue/db prisma:migrate",
    "dev": "pnpm --stream -r dev",
    "build:packages": "pnpm --stream -r install && pnpm prisma:migrate && pnpm --filter ./packages/** run build && pnpm --stream -r install",
    "start": "pnpm --stream -r start"
  },
  "devDependencies": {
    "@cut-the-queue/eslint-config": "workspace:*",
    "@cut-the-queue/typescript-config": "workspace:*",
    "prettier": "^3.2.5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
